description: >
  Installs python
# What will this command do?
# Descriptions should be short, simple, and clear.
steps:
  - run:
      name: Install Python
      command: <<include(scripts/InstallPython.sh)>>
  - run:
      name: Install Pip && a dependency 
      command: <<include(scripts/pip_install.sh)>>
