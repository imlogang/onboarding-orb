description: >
  Say hello world
# What will this job do?

executor: default

parameters:
    para1:
      default: "Hello"
      type: string
    para2:
      default: "abc"
      type: string
    para3:
      default: 2020
      type: integer
    executors:
      default: default
      type: executor
steps:
  - run: echo << parameters.para1 >>
  - run: echo << parameters.para2 >>
  - run: echo << parameters.para3 >>
