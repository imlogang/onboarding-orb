description: >
  Say hello world
# What will this job do?

executor: default
circleci_ip_ranges: << parameters.ip_ranges >>

parameters:
  ip_ranges:
    type: boolean
    default: false

steps:
  - run: echo "hello"
