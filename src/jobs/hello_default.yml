description: >
  Say hello world
# What will this job do?

executor: default-<<parameters.para2>>-<<parameters.para3>>
  
parameters:
  para1:
    default: "Hello"
    type: string
  para2:
    default: "abc"
    type: string
  para3:
    default: "2020"
    type: string

steps:
  - run: echo "hello"
